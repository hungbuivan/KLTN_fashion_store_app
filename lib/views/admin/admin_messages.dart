import 'package:flutter/material.dart';

class AdminMessages extends StatelessWidget {
  const AdminMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('💬 Tin nhắn với người dùng sẽ hiển thị tại đây.'),
    );
  }
}
